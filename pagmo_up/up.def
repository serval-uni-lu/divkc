BootStrap: docker
From: debian:12

%files
    ./ /up

%post
    echo "setup start"
    apt-get -y update
    apt-get -y install make g++ libpagmo-dev

    cd up
    make clean
    make -j8

    ls
    echo "setup done"

%runscript
    /up/up "$@"
